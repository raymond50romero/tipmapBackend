export function getWeights(posts) {
  if (!posts) return false;

  // first find the mean rbar
  let r_bar = 0;
  for (let i = 0; i < posts.length; i++) {}
}

export function getGlobalMean(posts) {
  if (!posts) return false;

  const muArray = [];

  for (let i = 0; i < posts.length; i++) {}
}
